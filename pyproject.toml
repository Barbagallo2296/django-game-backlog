[project]
name = "django-game-backlog"
version = "0.1.0"
description = "Backend Django + DRF per gestire un backlog di videogiochi"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.7",
    "djangorestframework>=3.17.1",
]
